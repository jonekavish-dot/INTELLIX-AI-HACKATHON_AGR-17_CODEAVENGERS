# ==============================================================================
# AgriDiff AI — Environment Configuration Template (.env.example.txt)
# Agricultural Document Comparison & Change Intelligence Engine
# Team CODEAVENGERS | BIT-AI-001 | AGR-17
# ==============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env:
#    Windows:   copy .env.example.txt .env
#    Linux/Mac: cp .env.example.txt .env
# 2. Populate your actual values below.
# 3. NEVER commit your actual .env file or real secrets to Git.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Google Gemini Generative AI Configuration
# ------------------------------------------------------------------------------
# Required for LLM-assisted change narrative generation and reasoning.
# If left empty or omitted, AgriDiff AI automatically falls back to its
# deterministic rule-based semantic alignment and extraction pipeline.
GEMINI_API_KEY=<YOUR_GEMINI_API_KEY_HERE>

# Gemini model identifier (default: gemini-1.5-flash)
# Supported options: gemini-1.5-flash, gemini-1.5-pro
LLM_MODEL=gemini-1.5-flash

# Timeout in seconds for remote LLM API calls before triggering deterministic fallback
LLM_TIMEOUT_SECONDS=30

# Maximum concurrent async requests dispatched to the LLM API
LLM_MAX_CONCURRENT=5

# ------------------------------------------------------------------------------
# 2. Server & Networking Configuration
# ------------------------------------------------------------------------------
# Port for FastAPI backend service (Render sets this dynamically via $PORT)
PORT=8000

# ------------------------------------------------------------------------------
# 3. Frontend Production Configuration (Vite)
# ------------------------------------------------------------------------------
# Base URL of the backend API service.
# - In local development: Leave blank (Vite dev proxy forwards /api to http://localhost:8000).
# - In production (e.g. Vercel): Set to your Render backend URL, e.g. https://agridiff-backend.onrender.com
VITE_API_URL=
